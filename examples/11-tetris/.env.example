# Environment (dev or prod)
ENVIRONMENT=dev

# GitHub OAuth (for FastAPI backend)
# REQUIRED: Create a GitHub OAuth App at https://github.com/settings/developers
# Set Authorization callback URL to: http://localhost:3000/api/auth/callback
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret

# REQUIRED: Generate a secret key for JWT token signing
# Run: openssl rand -base64 32
SECRET_KEY=generate-a-secret-with-openssl-rand-base64-32

# OPTIONAL: Base URL for OAuth redirects
# If not set, will automatically use the request's base URL
# BASE_URL=http://localhost:3000

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/tetris
