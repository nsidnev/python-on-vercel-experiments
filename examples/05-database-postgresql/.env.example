# PostgreSQL Database URL
# Format: postgresql://user:password@host:port/database

# Option 1: Local PostgreSQL (via docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vercel_db

# Option 2: Neon (serverless PostgreSQL for production)
# DATABASE_URL=postgresql://user:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Get your Neon database URL from:
# 1. Create account at https://neon.tech
# 2. Create a new project
# 3. Copy the connection string from the dashboard
# 4. Add it to Vercel: vercel env add DATABASE_URL
