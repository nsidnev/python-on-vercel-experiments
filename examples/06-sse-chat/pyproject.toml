[project]
name = "sse-timestamp-stream"
version = "0.1.0"
description = "Minimal SSE streaming example with FastAPI"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.0.0",
    "uvicorn>=0.32.0",
]
